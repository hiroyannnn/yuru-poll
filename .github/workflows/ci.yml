name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install MoonBit
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"
      - name: Install dependencies
        run: moon update && moon install
      - name: Check
        run: moon check --target all
      - name: Interfaces and formatting are committed
        run: moon info && moon fmt && git diff --exit-code
      - name: Embedded assets are up to date
        run: python3 scripts/embed_assets.py --check
      - name: Test
        run: moon test --target all
      - name: Workers glue parses
        run: node --check cloudflare/src/index.mjs
